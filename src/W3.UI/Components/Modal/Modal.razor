@namespace W3.UI.Components

@if (IsVisible)
{
    <div class="modal">

        <!-- Modal content -->
        <div class="content">
            @ChildContent
        </div>

    </div>
}

@code {
    [Parameter]
    public bool IsVisible { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}